import { LoginForm } from "@/components/login-form.tsx";

const Index = () => {
  return (
    <section className="container mx-auto flex min-h-screen flex-col items-center justify-center">
      <LoginForm />
    </section>
  );
};
export default Index;
